#🎥 Bluesney Movie App

A simple movie listing web app built with **HTML**, **CSS**, and **JavaScript**.  
I created this project to practice consuming a public movie API and manipulating the DOM to display dynamic content on the page.

🔗 **Live demo:** https://guilhermedev25.github.io/bluesney-movie-project/  
🔗 **Repository:** https://github.com/Guilhermedev25/bluesney-movie-project  

---

## ✨ Features

- Fetches movie data from a **public movie API**
- Displays **poster, title and rating** for each movie
- Layout built with **Flexbox** and `flex-wrap` to create a responsive card grid
- Simple and clean UI with the **Bluesney** brand
- Search bar to filter movies by title

---

## 🛠 Tech Stack

- **HTML5** – page structure and semantic elements  
- **CSS3** – layout and styling  
  - Flexbox + `flex-wrap` for the movie grid  
- **JavaScript (ES6+)**  
  - `fetch()` to call the movie API  
  - DOM manipulation to create and insert movie cards dynamically  

---

## 📂 Project Structure

```text
.
├── index.html          # Main HTML page
├── css/
│   └── style.css       # Layout, colors and responsive styles
├── js/
│   └── script.js       # API calls and DOM manipulation
└── img/                # Logo and images